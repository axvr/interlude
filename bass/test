#!/usr/bin/env bass

(use (*dir*/refrain.bass))

(defn run-checks [src]
  (-> src (refrain:checks) vals list->source (each succeeds?)))

(defn main args
  (for [{:src src} *stdin*]
    (run-checks src)))
